# Pinned CLIENT_DOMAINS for the eod-drafter test harness. Matches the
# SKILL.md §"Preflight" default exactly, but pinned here (instead of relying
# on the shell default) so a change to the real default doesn't silently
# change what these tests exercise.
CLIENT_DOMAINS=natera.com,goengen.com
